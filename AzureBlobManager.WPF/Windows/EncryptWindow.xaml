<Window x:Class="AzureBlobManager.Windows.EncryptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:AzureBlobManager.Controls"
        Icon="/Images/CloudFileServiceConfiguration.ico"
        mc:Ignorable="d"
        MinWidth="490"
        MinHeight="400"
        Background="#7392B7"
        Title="Encrypt Window - My Azure Blob Manager" Height="460" Width="573" >
 
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/ButtonStyles.xaml"/>
                <ResourceDictionary Source="../Styles/LabelStyles.xaml"/>
                <ResourceDictionary Source="../Styles/DataGridStyles.xaml"/>
                <ResourceDictionary Source="../Styles/TextBoxStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="46"/>
            <RowDefinition Height="190"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Password Source:" HorizontalAlignment="Right" Margin="0,13,350,0" VerticalAlignment="Top" Padding="2,5,5,5"   Grid.Row="0" />
        <ComboBox x:Name="cmbPasswordSource" HorizontalAlignment="Right" Margin="0,13,230,0" VerticalAlignment="Top" Width="120" FontSize="14" Height="28"  Grid.Row="0"/>

        <Button x:Name="btnSample" Click="btnSample_Click"  Margin="0,10,180,0" ToolTip="Get Sample Text"  VerticalAlignment="Top" HorizontalAlignment="Right"
                Grid.Row="0">
            <controls:AddSampleTextViewbox />
        </Button>

        <Button x:Name="btnClear" Click="btnClear_Click" Margin="0,10,140,0" ToolTip="Clear All Text"  VerticalAlignment="Top" HorizontalAlignment="Right"
                Grid.Row="0">
            <controls:CloseViewbox />
        </Button>
        <Button x:Name="btnExportKey" Click="btnExportKey_Click"  Margin="0,10,100,0" ToolTip="Export Key"  VerticalAlignment="Top" HorizontalAlignment="Right" Grid.Row="0" >
            <controls:ExportKeyViewbox />
        </Button>
        <Button x:Name="btnEncrypt" Click="btnEncrypt_Click"  Margin="0,10,60,0" ToolTip="Encrypt"  VerticalAlignment="Top" HorizontalAlignment="Right"  Grid.Row="0">
            <controls:EncryptButtonViewbox />
        </Button>
        <Button x:Name="btnDecrypt" Click="btnDecrypt_Click"  Margin="0,10,20,0" ToolTip="Decrypt"  VerticalAlignment="Top" HorizontalAlignment="Right"  Grid.Row="0" >
            <controls:DecryptButtonViewbox/>
        </Button>

        <Label Content="Plain Text" HorizontalAlignment="Left" Margin="20,0,0,0" VerticalAlignment="Top" Padding="2,2,5,5" Grid.Row="1" />
        <TextBox x:Name="txtPlainText" Margin="20,25,20,0" Height="165" Style="{StaticResource MultilineTextBox}" VerticalAlignment="Top"  HorizontalAlignment="Stretch" Grid.Row="1"   />

        
        <Label Content="Cypher Text" HorizontalAlignment="Left" Margin="20,0,0,0" VerticalAlignment="Top" Padding="2,9,5,5" Grid.Row="2" />
        <TextBox x:Name="txtCypherText" Margin="20,30,20,15" Style="{StaticResource MultilineTextBox}" Grid.Row="2" />

    </Grid>
</Window>
